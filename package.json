{
  "name": "newssite",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix version-4",
    "build": "npm run build --prefix version-4",
    "preview": "npm run preview --prefix version-4",
    "lint": "npm run lint --prefix version-4",
    "install:app": "npm install --prefix version-4",
    "postinstall": "npm install --prefix version-4"
  }
}
